[package]
name = "krea2-abi"
version = "0.1.0"
edition = "2021"
description = "libkrea2.so: the block session and the pipeline behind their C ABIs"
license = "MIT"
publish = false

[lib]
name = "krea2"
crate-type = ["cdylib"]

[dependencies]
krea2-pipeline = { path = "../pipeline" }
krea2-session = { path = "../session" }

[build-dependencies]
krea2-build-support = { path = "../build-support" }
cbindgen = "0.29.4"
