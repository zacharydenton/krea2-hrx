[package]
name = "abi-blocks"
version = "0.1.0"
edition = "2021"
description = "build/libkrea2.so: the block session behind its C ABI"
license = "MIT"
publish = false

[lib]
name = "krea2"
crate-type = ["cdylib"]

[dependencies]
krea2-session = { path = "../session" }
