[package]
name = "krea2-session"
version = "0.1.0"
edition = "2021"
description = "The 28 Krea 2 transformer blocks as a resident Loom session"
license = "MIT"
publish = false

[lib]
name = "krea2_session"

[dependencies]
bytemuck = "1"
hrx.workspace = true
krea2-kernels = { path = "../kernels" }
krea2-checkpoint = { path = "../checkpoint" }
